# `mimo-v2.5` 基础流式探针证据

- 日期：2026-06-09
- 模型：`mimo-v2.5`
- 探针命令：`npm run probe -- baseline`
- 结果：通过
- 证据来源：用户提供的终端输出截图

## 已确认行为

- 有效凭据可以访问默认 MiMo API 端点。
- API 返回流式推理增量和流式文本增量。
- 最终助手消息同时包含 `content` 与 `reasoning_content`。
- 响应结束原因归一化为 `stop`。
- 成功捕获响应 ID：`d4a1313745b747a8906e9b6faed2cd8d`。
- 成功捕获流式用量信息。
- `completion_tokens_details.reasoning_tokens` 单独报告推理 Token。

## 用量结果

| 字段 | 值 |
| --- | ---: |
| `prompt_tokens` | 43 |
| `completion_tokens` | 86 |
| `completion_tokens_details.reasoning_tokens` | 75 |
| `total_tokens` | 129 |

## 尚不能由该证据确认

- 原始 SSE wire 事件的完整结构。
- 非流式响应结构。
- 用户取消后的连接关闭行为。
- 工具调用行为。
- `reasoning_content` 的后续轮次重放要求。
- 高级模型 `mimo-v2.5-pro` 的行为。

## 发现的问题

旧版 CLI 在每个推理增量前重复打印 `[推理]`，影响可读性。该展示问题已修复，
现在每段连续推理只显示一次标题。
