# `mimo-v2.5` 工具恢复与同响应多工具调用证据

- 日期：2026-06-09
- 模型：`mimo-v2.5`
- 结果：三项均通过
- 证据来源：自动生成的脱敏报告

## 权限拒绝恢复

命令：`npm run probe -- recovery-loop --scenario denial`

| 项目 | 值 |
| --- | --- |
| 请求数 | 2 |
| 工具调用数 | 1 |
| 预期错误代码 | `permission_denied` |
| 是否观察到预期错误 | 是 |
| 是否重复调用不可重试工具 | 否 |
| 最终是否生成文本回答 | 是 |
| 结束原因 | `tool_calls`，之后 `stop` |

结论：默认模型接受结构化权限拒绝结果，不会自动重复被拒绝的操作，并能继续回答。

## 工具执行失败恢复

命令：`npm run probe -- recovery-loop --scenario failure`

| 项目 | 值 |
| --- | --- |
| 请求数 | 2 |
| 工具调用数 | 1 |
| 预期错误代码 | `tool_execution_failed` |
| 是否观察到预期错误 | 是 |
| 是否重复调用不可重试工具 | 否 |
| 最终是否生成文本回答 | 是 |
| 结束原因 | `tool_calls`，之后 `stop` |

结论：默认模型接受结构化工具执行失败结果，不会自动重复不可重试工具，并能解释失败。

## 同一助手响应内的多个工具调用

命令：`npm run probe -- parallel-tool-loop`

| 项目 | 值 |
| --- | ---: |
| 请求数 | 2 |
| 工具调用总数 | 2 |
| 单个助手响应内最大工具调用数 | 2 |
| 工具结果数 | 2 |
| 工具调用与结果 ID 是否全部匹配 | 是 |
| 最终是否生成文本回答 | 是 |
| 结束原因 | `tool_calls`，之后 `stop` |

结论：默认模型支持在同一个助手响应中发出多个工具调用，当前流式组装器能够正确
重建两个调用并关联结果。

## 安全检查

- 三份脱敏报告均未发现疑似 API 凭据。
- 三份脱敏报告均不包含消息正文或推理正文。
