# `mimo-v2.5` 进程重启恢复证据

- 日期：2026-06-09
- 模型：`mimo-v2.5`
- 探针命令：`npm run probe -- resume-latest "请确认刚才的工具调用结论，并说明使用过哪些工具"`
- 结果：通过
- 证据来源：自动生成的脱敏恢复报告与更新后的持久化会话

## 已确认行为

- 探针在新进程中读取并重放了包含工具历史的会话。
- 重放前会话包含 9 条消息，其中包含三次工具调用、三条工具结果和完整
  `reasoning_content`。
- MiMo API 接受了完整历史，没有返回 400 协议错误。
- 恢复请求成功生成一条新的助手消息和一条新的推理内容。
- 恢复后会话从 9 条消息增加至 11 条消息。
- 恢复请求未重复调用历史工具。
- 更新后的会话中，工具调用 ID 与工具结果 ID 仍全部匹配。
- 更新后的会话和脱敏报告均未发现疑似 API 凭据。

## 脱敏恢复报告结果

| 项目 | 值 |
| --- | ---: |
| 恢复前消息数 | 9 |
| 恢复后消息数 | 11 |
| 新增消息数 | 2 |
| 恢复请求数 | 1 |
| 新增助手消息数 | 1 |
| 新增推理消息数 | 1 |
| 新增工具调用数 | 0 |
| 结束原因 | `stop` |
| `prompt_tokens` | 1012 |
| `completion_tokens` | 519 |
| `reasoning_tokens` | 254 |
| `cached_tokens` | 768 |
| `total_tokens` | 1531 |

## 结论

默认模型已经通过一次真实的进程重启后会话恢复。该结果证明当前保存的
`reasoning_content`、工具调用和工具结果结构足以让 MiMo 接受后续请求。

仍需验证更长的多次恢复链、故意缺失 `reasoning_content` 的失败行为，以及中断状态恢复。
