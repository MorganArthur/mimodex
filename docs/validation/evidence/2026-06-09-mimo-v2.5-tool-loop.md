# `mimo-v2.5` 真实工具循环证据

- 日期：2026-06-09
- 模型：`mimo-v2.5`
- 探针命令：`npm run probe -- tool-loop`
- 结果：通过
- 证据来源：持久化会话文件与用户终端截图
- 会话文件：`spikes/mimo-provider/artifacts/session-2026-06-09T03-13-15Z.json`

## 已确认行为

- 默认模型能够生成真实工具调用。
- 完成了三次连续工具调用：
  1. `search_fixture`
  2. `read_fixture_file`
  3. `read_fixture_file`
- 三次工具调用都使用稳定的 `call_*` ID。
- 三条工具结果均与对应工具调用 ID 正确关联。
- 三个工具调用轮次的结束原因均为 `tool_calls`。
- 最终轮次结束原因为 `stop`，并给出正确的 Bug 原因与修复建议。
- 所有四条助手消息均保存了 `reasoning_content`。
- 会话成功保存，可用于后续进程重启恢复测试。
- 从第二次请求开始观察到 `prompt_tokens_details.cached_tokens`。

## 持久化会话检查

| 项目 | 值 |
| --- | ---: |
| 消息总数 | 9 |
| 助手消息数 | 4 |
| 包含推理内容的助手消息数 | 4 |
| 工具调用数 | 3 |
| 工具结果数 | 3 |
| 工具调用与结果 ID 是否匹配 | 是 |
| 最后一条消息角色 | `assistant` |

## 截图中可确认的请求用量

| 请求 | 结束原因 | 输入 Token | 输出 Token | 推理 Token | 总 Token | Cached Token |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| 1 | `tool_calls` | 423 | 65 | 43 | 488 | 未观察到 |
| 2 | `tool_calls` | 619 | 38 | 12 | 657 | 138 |
| 3 | `tool_calls` | 730 | 31 | 6 | 761 | 576 |
| 4 | `stop` | 截图未显示 | 截图未显示 | 截图未显示 | 截图未显示 | 截图未显示 |

## 尚未验证

- 使用该会话文件在新进程中发送跟进请求。
- 五轮工具型会话重放。
- 工具拒绝、失败、超时与取消后的模型恢复。
- 同一助手响应内的多个并行工具调用。
